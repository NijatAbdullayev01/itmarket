import { PageLoading } from "@itmarket/ui";

/** Panel title chrome reserved — matches real h1.ui-page-title--panel geometry. */
export default function Loading() {
  return <PageLoading variant="compare" />;
}
